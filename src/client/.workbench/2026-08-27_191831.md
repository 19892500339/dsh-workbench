# 📇 代码索引 · E:\dsh_work\dsh-workbench\src\client · 2026-08-27_191831

> 由 dsh-workbench `workbench_code_index` 生成 · 每块记录「文件 + 起始行/结束行」· 检索直达请用 `workbench_code_locate` · 本目录最新索引始终在 `latest.md`

## 🔄 本次变更
- 新增: 46 · 更新: 0 · 移除: 40 · 当前块总数: 63
- 说明: 工作台 UI 迁移到 DSW design token：palette 改为 var(--dsw-alias-*)、emoji 替换为 DSH 内置图标、LogicFlow 画布主题感知。新增 theme.ts 与 shims/dsh-primitives.d.ts。

## 📄 文件清单
| 文件 | 块数 | 行数 |
|---|---|---|
| `ErrorBoundary.tsx` | 6 | 48 |
| `MechanismBar.tsx` | 6 | 258 |
| `PromptQuickBar.tsx` | 5 | 187 |
| `WorkbenchView.tsx` | 2 | 106 |
| `WorkflowGraph.tsx` | 15 | 520 |
| `api.ts` | 4 | 30 |
| `i18n.ts` | 10 | 295 |
| `index.tsx` | 3 | 74 |
| `theme.ts` | 5 | 69 |
| `ui.tsx` | 7 | 137 |

## 🧩 功能块
### `ErrorBoundary.tsx`
#### 🧬 类型 `ErrorBoundaryProps` · L8-L11
- 签名: `interface ErrorBoundaryProps {`

#### 🧬 类型 `ErrorBoundaryState` · L13-L15
- 签名: `interface ErrorBoundaryState {`

#### 🏷️ 类 `ErrorBoundary` · L17-L47
- 签名: `export class ErrorBoundary extends React.Component<ErrorBoundaryProps, ErrorBoundaryState>`

#### ⚙️ 方法 `getDerivedStateFromError` · L20-L22
- 签名: `static getDerivedStateFromError(error: Error): ErrorBoundaryState {`

#### ⚙️ 方法 `componentDidCatch` · L24-L26
- 签名: `override componentDidCatch(error: Error): void {`

#### ⚙️ 方法 `render` · L28-L46
- 签名: `override render(): React.ReactNode {`

### `MechanismBar.tsx`
#### 🧬 类型 `Domain` · L22-L24
- 签名: `type Domain = keyof MechanismOverrides`

#### 🔧 函数 `MechanismBar` · L51-L209
- 签名: `export function MechanismBar(): React.ReactElement | null {`

#### 📦 常量 `onDown` · L73-L75
- 签名: `const onDown = (event: MouseEvent) => {`

#### 🔧 函数 `choose` · L86-L96
- 签名: `async function choose(domain: Domain, mode: OverrideMode | RagOverrideMode) {`

#### 🔧 函数 `pickRagTarget` · L99-L115
- 签名: `async function pickRagTarget(kbId: string) {`

#### 🔧 函数 `resetDomain` · L117-L127
- 签名: `async function resetDomain(domain: Domain) {`

### `PromptQuickBar.tsx`
#### 🔧 函数 `PromptQuickBar` · L15-L132
- 签名: `export function PromptQuickBar(): React.ReactElement | null {`

#### 📦 常量 `onDown` · L37-L39
- 签名: `const onDown = (event: MouseEvent) => {`

#### 🔧 函数 `pick` · L52-L63
- 签名: `async function pick(id: string) {`

#### 🔧 函数 `clear` · L65-L75
- 签名: `async function clear() {`

#### 🔧 函数 `injectedPreview` · L135-L138
- 签名: `function injectedPreview(p: { name: string; content: string }): string {`

### `WorkbenchView.tsx`
#### 🧬 类型 `ModuleId` · L26-L28
- 签名: `type ModuleId = 'rag' | 'mcp' | 'workflow' | 'skill' | 'tool' | 'prompt'`

#### 🔧 函数 `WorkbenchView` · L37-L105
- 签名: `export function WorkbenchView(props: { sessionId?: string }) {`

### `WorkflowGraph.tsx`
#### 🧬 类型 `WorkflowGraphProps` · L29-L41
- 签名: `export interface WorkflowGraphProps {`

#### 🔧 函数 `applyEdgeTheme` · L49-L55
- 签名: `function applyEdgeTheme(lf: InstanceType<typeof LogicFlowDefault>): void {`
- 功能: 用解析后的主题颜色重设 LogicFlow 连线(line/polyline/bezier)描边。
- 入参: lf(LogicFlow 实例)
- 返回: 无
- 副作用: 调用 lf.setTheme
- 依赖: canvasTheme

#### 🏷️ 类 `WbNodeModel` · L74-L91
- 签名: `class WbNodeModel extends RectNodeModel {`

#### ⚙️ 方法 `initNodeData` · L75-L79
- 签名: `override initNodeData(data: any): void {`

#### ⚙️ 方法 `setAttributes` · L80-L90
- 签名: `override setAttributes(): void {`
- 功能: 按节点 kind 与选中态设置节点填充/描边，颜色取自主题感知的 canvasTheme。
- 入参: 无(读 this.properties)
- 返回: 无
- 副作用: 写 this.fill/stroke/strokeWidth/radius
- 依赖: canvasTheme, KIND_COLOR

#### 🏷️ 类 `WbNodeView` · L94-L144
- 签名: `class WbNodeView extends RectNode {`

#### ⚙️ 方法 `getShape` · L95-L143
- 签名: `override getShape(): ReturnType<typeof h> {`
- 功能: 渲染自定义工作流节点(矩形+标题+摘要)，文字颜色用 canvasTheme 的 text/dim。
- 入参: 无(读 model)
- 返回: SVG vnode 树
- 副作用: 无
- 依赖: canvasTheme

#### 🔧 函数 `NodeEditor` · L147-L192
- 签名: `function NodeEditor(props: {`

#### 📦 常量 `patch` · L156-L163
- 签名: `const patch = (p: Partial<WorkflowNode>) => onUpdate(node.id, p)`

#### 📦 常量 `params` · L157-L163
- 签名: `const params = (next: Record<string, string>) => patch({ params: next })`

#### 🔧 函数 `WorkflowGraph` · L258-L258
- 签名: `export function WorkflowGraph(props: WorkflowGraphProps) {`

#### 📦 常量 `onKey` · L294-L296
- 签名: `const onKey = (event: KeyboardEvent) => {`

#### 📦 常量 `orderByPositions` · L327-L335
- 签名: `const orderByPositions = () => {`

#### 📦 常量 `applyChainEdges` · L337-L347
- 签名: `const applyChainEdges = (order: WorkflowNode[]) => {`

#### 🔧 函数 `deleteSelected` · L412-L416
- 签名: `function deleteSelected() {`

### `api.ts`
#### 🏷️ 类 `WorkbenchApiError` · L5-L11
- 签名: `export class WorkbenchApiError extends Error {`

#### ⚙️ 方法 `constructor` · L7-L10
- 签名: `constructor(code: string, message: string) {`

#### 🔧 函数 `call` · L13-L25
- 签名: `export async function call<T>(method: string, payload: unknown = {}): Promise<T> {`

#### 🔧 函数 `errorMessage` · L27-L29
- 签名: `export function errorMessage(error: unknown): string {`

### `i18n.ts`
#### 🧬 类型 `Lang` · L9-L130
- 签名: `export type Lang = 'zh' | 'en'`

#### 🧬 类型 `Dict` · L11-L130
- 签名: `type Dict = Record<string, string>`

#### 🔧 函数 `setLang` · L244-L248
- 签名: `export function setLang(next: Lang): void {`

#### 🔧 函数 `getLang` · L250-L252
- 签名: `export function getLang(): Lang {`

#### 🔧 函数 `t` · L255-L262
- 签名: `export function t(key: string, vars?: Record<string, string | number>): string {`

#### 🔧 函数 `useLocale` · L265-L274
- 签名: `export function useLocale(): Lang {`

#### 🧬 类型 `LocaleServiceLike` · L277-L280
- 签名: `export interface LocaleServiceLike {`

#### ⚙️ 方法 `getLocale` · L278-L278
- 签名: `getLocale(): { active: string }`

#### 🔧 函数 `syncWithDshLocale` · L283-L294
- 签名: `export function syncWithDshLocale(locale: LocaleServiceLike | undefined): (() => void) | u`

#### 📦 常量 `apply` · L285-L291
- 签名: `const apply = () => {`

### `index.tsx`
#### 🧬 类型 `SlotsServiceLike` · L15-L18
- 签名: `interface SlotsServiceLike {`

#### 🧬 类型 `WorkbenchClientCtx` · L20-L24
- 签名: `interface WorkbenchClientCtx {`

#### 🔧 函数 `apply` · L26-L73
- 签名: `export function apply(ctx: WorkbenchClientCtx): void {`

### `theme.ts`
#### 🔧 函数 `readCssVar` · L20-L24
- 签名: `export function readCssVar(name: string, fallback: string): string {`
- 功能: 读取 body 上 CSS 自定义属性的计算值(含 var 替换)，返回具体颜色字符串；宿主未挂载时回退。
- 入参: name(变量名), fallback(回退色)
- 返回: 解析后的颜色字符串
- 副作用: 无
- 依赖: 无

#### 🔧 函数 `resolveCanvasTheme` · L27-L37
- 签名: `export function resolveCanvasTheme() {`
- 功能: 把 LogicFlow 画布需要的语义色 token 解析为具体颜色(panel/border/text/dim/accent/selected)。
- 入参: 无
- 返回: CanvasTheme 颜色对象
- 副作用: 读 DOM 计算样式
- 依赖: readCssVar

#### 🧬 类型 `CanvasTheme` · L39-L68
- 签名: `export type CanvasTheme = ReturnType<typeof resolveCanvasTheme>`

#### 🔧 函数 `useThemeVersion` · L46-L68
- 签名: `export function useThemeVersion(): number {`
- 功能: 主题切换时触发重渲染的 hook：监听 body[data-ds-dark-theme] 属性与 prefers-color-scheme。
- 入参: 无
- 返回: 自增版本号
- 副作用: 注册 MutationObserver 与媒体查询监听
- 依赖: 无

#### 📦 常量 `bump` · L49-L65
- 签名: `const bump = () => setVersion((v) => v + 1)`

### `ui.tsx`
#### 🔧 函数 `Section` · L79-L89
- 签名: `export function Section(props: { title: React.ReactNode; right?: React.ReactNode; children`

#### 🔧 函数 `Field` · L91-L98
- 签名: `export function Field(props: { label: string; children: React.ReactNode }) {`

#### 🔧 函数 `Button` · L100-L107
- 签名: `export function Button(props: React.ButtonHTMLAttributes<HTMLButtonElement> & { variant?: `

#### 🔧 函数 `Toggle` · L109-L116
- 签名: `export function Toggle(props: { checked: boolean; onChange: (next: boolean) => void; label`

#### 🔧 函数 `Empty` · L118-L120
- 签名: `export function Empty(props: { text: string }) {`

#### 🔧 函数 `ErrorNote` · L122-L128
- 签名: `export function ErrorNote(props: { text: string }) {`
- 功能: 错误提示条，用 IconWarningOutline16 图标 + danger token 颜色。
- 入参: text
- 返回: JSX
- 副作用: 无
- 依赖: styles, IconWarningOutline16

#### 🔧 函数 `okNote` · L130-L136
- 签名: `export function okNote(text: string) {`
- 功能: 成功提示文本，用 IconCheckOutline16 图标 + success token 颜色。
- 入参: text
- 返回: JSX
- 副作用: 无
- 依赖: styles, IconCheckOutline16
